name: OpenSrc
on:
  workflow_dispatch:
jobs:
  opensrc:
    runs-on: ubuntu-latest
    environment: WF_Env
    steps:
      - name: CONFIG
        run: git config --global user.name "sanchilon" && git config --global user.email "sandeep.chiluveru@ontario.ca"
      - uses: actions/checkout@v2
        with:
          repository: sanchilon/VerifyOpenSource
          path: ./VerifyOpenSource
          ref: refs/heads/main
          token: ${{ secrets.PAT }}
      - name: Checkout OntarioVerify
        uses: actions/checkout@v2
        with:
          repository: ongov/OntarioVerify
          path: ./OntarioVerify
          ref: refs/heads/main
          token: ${{ secrets.PAT }}
      - name: LIST
        run: ls -al ./VerifyOpenSource && ls -al ./OntarioVerify
      - name: RUN
        run: |
          cp -R ./OntarioVerify ./VerifyOpenSource/OntarioVerify && \
          rm -rf ./VerifyOpenSource/OntarioVerify/.git && \
          rm -rf ./VerifyOpenSource/OntarioVerify/.github && \
          ls -al ./VerifyOpenSource && \
          cp -R ./VerifyOpenSource/openverify ./VerifyOpenSource/OntarioVerify && \
          ls -al ./VerifyOpenSource/OntarioVerify/openverify
      - name: PUSH
        run: cd ./VerifyOpenSource && git switch -C test1 && git push -u origin test1
          
